[tools]
opam = "2.5.2"

[tasks.setup]
description = "Install Dune with opam, then build Dune-locked dependencies"
run = "tools/bootstrap"

[tasks.test]
description = "Run the HTTP harness"
run = "tools/harness run --tier fast"

[tasks."setup:afl"]
description = "Build the pinned native AFL++ tools"
run = "sh tools/setup-afl.sh"

[tasks.docs]
description = "Build API documentation with the locked odoc"
run = "tools/dune-pkg build @doc"

[tasks.bench]
description = "Run repeated release-profile benchmarks across all primitives"
run = "tools/dev bench"

[tasks."bench:core"]
run = "tools/dev bench --family core"

[tasks."bench:router"]
run = "tools/dev bench --family router"

[tasks."bench:parsers"]
run = "tools/dev bench --family http1"

[tasks."bench:middleware"]
run = "tools/dev bench --family middleware"

[tasks."bench:engine"]
run = "tools/dev bench --family engine"

[tasks."bench:compare"]
description = "Compare common workloads against Routes, http/af and httpun"
run = "tools/dev bench --external"

[tasks."bench:bodies"]
description = "Compare public body-reading APIs across framing and fragmentation variants"
run = "tools/dev bench --external --family body"

[tasks."bench:router-experiment"]
description = "Check and measure the benchmark-only route candidate index"
run = "tools/dev bench --external --family router-experiment"

[tasks."setup:nginx"]
description = "Build the pinned local Nginx interop reference"
run = "tools/setup-nginx.sh"

[tasks.interop]
description = "Run direct and real Nginx HTTP/1 interoperability lanes"
run = "tools/dev interop"

[tasks.performance]
description = "Measure streaming bounds and advisory native mixed workloads"
run = "tools/dev performance"

[tasks."bench:exchanges"]
description = "Compare public body writers and persistent request/response exchanges"
run = "tools/dev bench --external --family exchange"

[tasks."bench:profile-bodies"]
description = "Capture single-fixture body allocation, driver counts and OS memory diagnostics"
run = "tools/dev profile-bodies"
